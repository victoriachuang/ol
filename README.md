# ol

### Todo's as of May 7
* Write documentation

### Required gems

The API endpoints require the `sinatra` gem:
```
gem install sinatra
```

The tests require `rspec` and `rack-test`:
```
gem install rspec
gem install rack-test
```